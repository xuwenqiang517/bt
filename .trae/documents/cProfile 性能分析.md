## 性能分析计划

**问题定位：**
- 50策略 x 1周期 耗时 **3.31秒**
- 每次循环都创建新的 `UpStrategy` 对象
- `_init_pick_filter()` 和 `_init_pick_sorter()` 每次都创建新闭包

**优化方案：**
1. **复用策略对象** - 重置策略状态而不是创建新对象
2. **优化初始化** - 闭包改为直接内联逻辑，避免创建函数对象

**预期效果：**
- 从 ~15 it/s 提升到 30+ it/s